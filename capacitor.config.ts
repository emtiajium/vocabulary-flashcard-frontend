import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.emtiajium.ielts.gibberish.android',
    appName: 'IELTS Gibberish',
    webDir: 'dist',
    bundledWebRuntime: false,
};

export default config;
